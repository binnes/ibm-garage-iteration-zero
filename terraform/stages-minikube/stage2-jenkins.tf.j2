module "dev_tools_jenkins" {
  source = "git::https://gitea.{{ minikubeDomain }}/cloudNativeToolkit/terraform-tools-jenkins.git"

  cluster_ingress_hostname = var.ingress_hostname
  cluster_config_file      = var.config_file_path
  cluster_type             = var.cluster_type_code
  tls_secret_name          = var.tls_secret
  server_url               = var.server_url
  tools_namespace          = module.dev_tools_namespace.name
}
